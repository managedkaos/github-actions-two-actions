name: Test two actions
on:
  push:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * MON"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ./action1
      - uses: ./action2
